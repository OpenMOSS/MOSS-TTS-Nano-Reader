# Future Extension Integration

This directory is reserved for a future companion-aware extension variant.

The current source of truth for the browser extension is still the repository root `extension/` directory.

It is intentionally not duplicated yet to avoid divergence while the desktop app protocol is still being defined.
